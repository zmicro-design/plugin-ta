#!/bin/bash

ta::dev() {
  ta::prepare

  local key="DEV_SCRIPT"
  local cmd=$(ta::config ${key})

  if [ "$cmd" = "" ]; then
    npm run dev --if-present
    npm run start --if-present
  else
    npm run "$cmd"
  fi
}

export -f ta::dev
